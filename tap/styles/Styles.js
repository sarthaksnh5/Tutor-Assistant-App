import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  scrollContainer: {
    flexGrow: 1,
    alignItems: 'center',
  },
  styleScroll: {
    width: '100%',
  },
});
